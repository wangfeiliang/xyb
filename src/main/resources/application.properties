jdbc.driverClassName=${jdbc.driverClassName}
jdbc.url=${jdbc.url}
jdbc.username=${jdbc.username}
jdbc.password=${jdbc.password}
path.show=${path.show}
groupId=${groupId}
partifactId=${partifactId}
basedir=${basedir}
mirrors=${mirrors}
projectDirectory=${projectDirectory}